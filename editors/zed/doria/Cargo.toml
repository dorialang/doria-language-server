[package]
name = "zed_doria"
version = "2026.3.1-canary"
edition = "2021"
publish = false
license = "Apache-2.0"

[lib]
crate-type = ["cdylib"]

[dependencies]
zed_extension_api = "0.7.0"

# Stand alone from the doria-language-server Cargo workspace: Zed builds each
# extension as its own crate.
[workspace]
